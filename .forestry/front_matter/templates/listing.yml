---
label: Listing
hide_body: true
fields:
- name: listing
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: true
    label: name
    default: Default Name
  - name: email
    type: text
    config:
      required: true
    label: email
    default: username@example.com
  config:
    min: 
    max: 
    labelField: 
  label: Listing
